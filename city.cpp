#include <stdio.h>
#include "city.h"

void initialize(City *cityArray)
{
  city->longitude = 0;
  city->latitude = 0;
  city->name = NULL;
  city->state = NULL;
  city->airport[0] = '\0';
  city->population = 0;
} // initializes, sets NULL and 0 values to members of cityArray 

